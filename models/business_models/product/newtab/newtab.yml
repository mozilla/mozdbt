models:
  - name: newtab_visits
    latest_version: 1
    description: Visits on the newtab page, one row per visit per-day (identified by the visit id).
    versions:
      - v: 1
    config:
      publish_view: true
